{
  "name": "test-task",
  "version": "0.1.0",
  "scripts": {
    "start": "npm run protos-build && npm run start-client",
    "install-dependency": "npm run protos-dependency && npm run protos-build && npm run client-dependency",
    "protos-build": "cd ./protos && npm run build-copy",
    "start-client": "cd ./client && npm start",
    "protos-dependency": "cd ./protos && npm ci",
    "client-dependency": "cd ./client && npm ci"
  }
}
